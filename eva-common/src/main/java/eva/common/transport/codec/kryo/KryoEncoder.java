package eva.common.transport.codec.kryo;

import eva.common.transport.codec.MessageCodecUtil;
import eva.common.transport.codec.MessageEncoder;

public class KryoEncoder extends MessageEncoder {

    public KryoEncoder(MessageCodecUtil util) {
        super(util);
    }
}