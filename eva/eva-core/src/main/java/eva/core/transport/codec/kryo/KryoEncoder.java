package eva.core.transport.codec.kryo;

import eva.core.transport.codec.MessageCodecUtil;
import eva.core.transport.codec.MessageEncoder;

public class KryoEncoder extends MessageEncoder {

    public KryoEncoder(MessageCodecUtil util) {
        super(util);
    }
}